{
  "name": "kritx-learning-os",
  "version": "1.0.0",
  "description": "kritX - personal learning OS with tasks, Codex, and cloud sync. Visit a URL, log in, no install.",
  "private": true,
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server/index.js",
    "serve": "node server/index.js",
    "serve:static": "node scripts/dev-server.js",
    "cap:add": "cap add android",
    "cap:sync": "cap sync android",
    "apk:debug": "cap sync android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
